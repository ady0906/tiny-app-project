<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <% include ./partials/_header %>
  <ul>
  <% for (let pair in urls) { %>
      <li><%= pair %>: <%= urls[pair] %> &nbsp;
        <a href="/urls/<%= pair %>">Edit</a>
        <form id = "form1" method = "POST" action = "/urls/<%= pair %>/delete">
          <button type = "submit" class = "btn btn-danger btn-sm" form = "form1" value = "Delete">Delete</button>
        </form>
      </li><br>
  <% }; %>
  </ul>
  <blockquote><a href="/urls/new">Shorten new URL</a></blockquote>
</body>
</html>
